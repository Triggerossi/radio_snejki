Music Recommendation Radio
Интеллектуальная радиостанция с персонализированными рекомендациями на основе ваших музыкальных предпочтений. Система анализирует лайкнутые треки и предлагает похожую музыку, периодически разбавляя рекомендации новыми жанрами и исполнителями для расширения музыкального кругозора.

✨ Возможности
📻 Умные рекомендации — система изучает ваши предпочтения на основе лайков

🔀 Динамический микс — каждый 4-й трек отличается от привычных жанров

❤️ Лайк-система — отмечайте понравившиеся треки одним кликом

📚 История прослушивания — хранит последние 5 прослушанных треков

🎯 Персонализация — учитывает любимых исполнителей и жанры

🔄 Бесшовная смена треков — AJAX-обновление без перезагрузки страницы

🧹 Сброс предпочтений — возможность начать рекомендации с чистого листа

🔐 Аутентификация — регистрация и вход для сохранения предпочтений

🛠 Технологии
Бэкенд:

Python 3.8+

Django 4.x

SQLite (или другая БД)

Фронтенд:

HTML5, CSS3

JavaScript (нативный)

AJAX для динамических запросов

Дополнительно:

Django Authentication System

Pillow для работы с изображениями

Built-in Django ORM

🚀 Установка и запуск
1. Клонирование репозитория
bash
git clone <ссылка-на-ваш-репозиторий>
cd music-recommendation-radio
2. Создание виртуального окружения
bash
python -m venv venv
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate
3. Установка зависимостей
bash
pip install django pillow
4. Настройка базы данных
bash
python manage.py makemigrations
python manage.py migrate
5. Создание суперпользователя
bash
python manage.py createsuperuser
6. Загрузка тестовых данных (опционально)
Создайте файл populate_db.py в папке приложения или используйте админ-панель:

bash
python manage.py shell
# В интерактивной оболочке создайте авторов, жанры и песни
7. Запуск сервера
bash
python manage.py runserver
Приложение будет доступно по адресу: http://127.0.0.1:8000/

📊 ER-диаграмма базы данных
erDiagram
    User ||--o{ Like : "ставит"
    Song ||--o{ Like : "получает"
    Author ||--o{ Song : "создаёт"
    Genre ||--o{ Song : "относится к"
    
    User {
        int id PK
        string username
        string password
        datetime date_joined
    }
    
    Song {
        int id PK
        string title
        string file
        string cover
        int author_id FK
        int genre_id FK
    }
    
    Author {
        int id PK
        string name
    }
    
    Genre {
        int id PK
        string name
    }
    
    Like {
        int id PK
        int user_id FK
        int song_id FK
        datetime created_at
    }
📡 API Endpoints
Основные эндпоинты:
Метод	URL	Описание	Требует аутентификации
GET	/radio/	Главная страница радио	Да
POST	/login/	Вход в систему	Нет
POST	/register/	Регистрация	Нет
GET	/logout/	Выход	Да
POST	/like/<song_id>/	Поставить/убрать лайк	Да
GET	/next-song/	Получить следующий трек	Да
POST	/reset-likes/	Сбросить все лайки	Да
Пример запроса к /next-song/:
javascript
// JavaScript на клиенте
fetch('/next-song/?current_id=5', {
    method: 'GET',
    headers: {
        'X-CSRFToken': getCookie('csrftoken')
    }
})
.then(response => response.json())
.then(data => {
    console.log(data);
    // {
    //     "id": 42,
    //     "title": "Song Title",
    //     "author": "Artist Name",
    //     "genre": "Pop",
    //     "file_url": "/media/songs/song.mp3",
    //     "cover_url": "/media/covers/cover.jpg",
    //     "is_liked": true,
    //     "likes_count": 15
    // }
});
🏗 Архитектурная схема
text
┌─────────────────┐     HTTP/HTTPS     ┌─────────────────┐
│                 ├───────────────────>│                 │
│    Браузер      │                    │   Django App    │
│   (Frontend)    │<───────────────────┤    (Backend)    │
│                 │     JSON/HTML      │                 │
└─────────────────┘                    └────────┬────────┘
                                                │
                                                │ ORM
                                                ▼
                                        ┌─────────────────┐
                                        │                 │
                                        │   Database      │
                                        │   (SQLite)      │
                                        │                 │
                                        └─────────────────┘
Поток данных:

Пользователь открывает радио в браузере

Django отдаёт HTML-шаблон с базовыми данными

JavaScript загружает первый трек

При клике "Следующий трек" - AJAX-запрос к /next-song/

Сервер обрабатывает логику рекомендаций и возвращает JSON

Браузер обновляет интерфейс без перезагрузки страницы

📸 Скриншоты
Страница входа
https://docs/photo1.jpg
Простая форма входа с валидацией данных

Главное радио
https://docs/photo3.jpg
Основной интерфейс с плеером, обложкой и кнопками управления

Админ-панель
https://docs/photo2.jpg
Управление песнями, авторами, жанрами и лайками через Django Admin

📝 Ключевые функции с комментариями
python
def radio_view(request):
    """
    Основная view для радиостанции.
    Обрабатывает логику рекомендаций на основе истории лайков пользователя.
    
    Алгоритм:
    1. Собирает любимые жанры и исполнители
    2. Считает счётчик треков для определения, когда показывать разнообразие
    3. Каждый 4-й трек - из НЕлюбимых жанров
    4. Обновляет историю прослушивания в сессии
    5. Обрабатывает лайки при загрузке страницы
    """
    # ... код функции
    
@require_POST
@login_required
def like_song(request, song_id):
    """
    Обработка лайков через AJAX.
    Возвращает JSON-ответ для динамического обновления интерфейса.
    
    Особенности:
    - Добавляет лайкнутый трек в 'recently_liked' сессии
    - Предотвращает повторные лайки
    - Работает без перезагрузки страницы
    """
    # ... код функции

def next_song_data(request):
    """
    Генерирует следующий трек для воспроизведения.
    Основная логика рекомендательной системы.
    
    Фильтрация:
    - Исключает последние 5 прослушанных треков
    - Исключает последние 5 лайкнутых треков
    - Учитывает счётчик для разнообразия (каждый 4-й трек)
    """
    # ... код функции
⚙️ Конфигурация алгоритма рекомендаций
Настройки можно изменить в коде radio_view и next_song_data:

python
# Количество треков в истории
RECENTLY_PLAYED_LIMIT = 5
RECENTLY_LIKED_LIMIT = 5

# Частота разнообразных треков (каждый N-ый трек)
DIVERSITY_FREQUENCY = 4

# Стратегия при отсутствии рекомендаций
FALLBACK_STRATEGY = 'random_all'  # или 'popular', 'newest'